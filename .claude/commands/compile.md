---
description: Compila y empaqueta el JAR de BlazorSpy
---

Ejecuta el script de compilación y empaquetado:

```bash
bash .claude/compile.sh
```

Si hay errores de compilación, analízalos y corrige los archivos Java afectados antes de reintentar.
